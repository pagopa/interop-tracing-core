node-address {
  protocol = http
  host = localhost
  context-path = ""
}

queues {
  enriched-upload {
    fifo = false
    contentBasedDeduplication = true
  }

  tracings-upload {
    fifo = false
    contentBasedDeduplication = true
  }

  processing-results {
    fifo = false
    contentBasedDeduplication = true
  }
}
